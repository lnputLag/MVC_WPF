<Window x:Class="MVC_WPF.Views.Windows.Sandbox.WeatherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVC_WPF.Views.Windows.Sandbox"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Background="#16161c"
        Title="Погода" Height="450" Width="800" Icon="/Views/Image/icon.png"
        Closing="Main_Window_Closing">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1">
            <RadioButton Style="{StaticResource MenuItem}" x:Name="MainScreen" Content="Главная" Checked="RadioButton_Checked" />
            <RadioButton Style="{StaticResource MenuItem}" x:Name="NotesScreen" Content="Заметки" Checked="RadioButton_Checked" />
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" x:Name="MainScreenPanel" Visibility="Hidden">
            <Image Source="/Views/Image/weather.png" Height="50" Width="50" Margin="0,50,0,0" />
            <Label Margin="0,20,0,30" Content="Погоное приложение" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="22" Foreground="White"/>
            <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="White" materialDesign:HintAssist.Hint="Введите город" x:Name="UserCityTextBox" HorizontalAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Top" Width="231"/>
            <Button Margin="0,30,0,0" x:Name="GetWeatherBtn" Content="Получить погоду" HorizontalAlignment="Center" VerticalAlignment="Top" Click="GetWeatherBtn_Click"/>
            <Label x:Name="WeatherResults" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,0" VerticalAlignment="Top"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" x:Name="NotesScreenPanel" >
            <Menu Background="#16161c" Foreground="White">
                <MenuItem Header="Файл" Foreground="White" x:Name="MenuFile">
                    <MenuItem Header="Новый файл" Click="MenuNewFile_Click" x:Name="MenuNewFile" Foreground="White">
                        <MenuItem.Icon>
                            <Image Source="/Views/Image/new_file.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Открыть файл" Click="MenuOpenFile_Click" x:Name="MenuOpenFile" Foreground="White">
                        <MenuItem.Icon>
                            <Image Source="/Views/Image/open_file.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Сохранить файл" Click="MenuSaveFile_Click" x:Name="MenuSaveFile" Foreground="White">
                        <MenuItem.Icon>
                            <Image Source="/Views/Image/save_file.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>

                <MenuItem Header="Редактировать" x:Name="MenuEdit" Foreground="White">
                    <MenuItem Header="Шрифт" x:Name="MenuSetFont" Foreground="White">
                        <MenuItem Header="Times New Roman" Click="TimesNewRomanSetText_Click" x:Name="TimesNewRomanSetText" StaysOpenOnClick="True" IsCheckable="True" />
                        <MenuItem Header="Verdana" Click="VerdanaSetText_Click" x:Name="VerdanaSetText" StaysOpenOnClick="True" IsCheckable="True" />
                    </MenuItem>
                    <Separator />
                    <MenuItem Foreground="White" x:Name="MenuCopyText" Header="Скопировать" Command="ApplicationCommands.Copy" InputGestureText="Cntrl+C" Cursor="" />
                    <MenuItem Foreground="White" x:Name="MenuCutText" Header="Вырезать" Command="ApplicationCommands.Cut" InputGestureText="Cntrl+X" />
                    <MenuItem Foreground="White" x:Name="MenuPasteText" Header="Вставить" Command="ApplicationCommands.Paste" InputGestureText="Cntrl+V" />
                </MenuItem>
            </Menu>
            <ToolBarTray Background="Transparent">
                <ToolBar Background="Transparent">
                    <Button ToolTip="Сохранить файл" Style="{StaticResource MaterialDesignFlatAccentButton}" Click="MenuSaveFile_Click">
                        <Image Source="/Views/Image/save_file.png" Width="25" Height="25" />
                    </Button>
                    <Button ToolTip="Открыть файл" Style="{StaticResource MaterialDesignFlatAccentButton}" Click="MenuOpenFile_Click">
                        <Image Source="/Views/Image/open_file.png" Width="25" Height="25" />
                    </Button>
                    <Button ToolTip="Вырезать" Style="{StaticResource MaterialDesignFlatAccentButton}" Command="ApplicationCommands.Cut">
                        <Image Source="/Views/Image/cut_icon.png" Width="25" Height="25" />
                    </Button>
                    <Button ToolTip="Вставить" Style="{StaticResource MaterialDesignFlatAccentButton}" Command="ApplicationCommands.Paste">
                        <Image Source="/Views/Image/paste_icon.png" Width="25" Height="25" />
                    </Button>
                    <Label Content="Размер шрифта:" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0" />
                    <ComboBox Foreground="White" x:Name="SelectFontSize" SelectionChanged="SelectFontSize_SelectionChanged">
                        <ComboBoxItem Tag="10" Foreground="Black" Content="10px" />
                        <ComboBoxItem Tag="12" Foreground="Black" x:Name="SetDefaultSize" Content="12px" />
                        <ComboBoxItem Tag="14" Foreground="Black" Content="14px" />
                        <ComboBoxItem Tag="16" Foreground="Black" Content="16px" />
                        <ComboBoxItem Tag="20" Foreground="Black" Content="20px" />
                    </ComboBox>
                </ToolBar>
            </ToolBarTray>
            <TextBox FontSize="12" x:Name="UserNotesTextBox" Margin="10" Style="{StaticResource MaterialDesignFilledTextFieldTextBox}" materialDesign:HintAssist.Hint="Ваша заметка тут" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" MaxHeight="200" Foreground="White"  />
        </StackPanel>
    </Grid>
</Window>
