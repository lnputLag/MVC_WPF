<Page x:Class="MVC_WPF.Views.Pages.Sandbox.WeatherApp"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MVC_WPF.Views.Pages.Sandbox"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WeatherApp">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Погодная страница"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <TextBox x:Name="UserCityTextBox"
                 Grid.Row="1"
                 Width="280"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Margin="0,0,0,15"
                 />

        <Button x:Name="GetWeatherBtn"
                Grid.Row="2"
                Content="Получить погоду"
                Width="150"
                HorizontalAlignment="Center"
                Margin="0,0,0,15"
                Click="GetWeatherBtn_Click"/>

        <Border Grid.Row="3"
                CornerRadius="8"
                BorderBrush="LightGray"
                BorderThickness="1"
                Padding="10"
                Background="#F9F9F9"
                Width="280"
                HorizontalAlignment="Center">
            <TextBlock x:Name="WeatherResults"
                       Text=""
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       FontSize="14"
                       Foreground="Black"/>
        </Border>
    </Grid>
</Page>
